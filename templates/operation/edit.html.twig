{% extends 'base.html.twig' %}

{% block title %}
  Edit Operation
{% endblock %}

{% block body %}
  <div class="d-flex justify-content-between">
    <a href="{{ path('app_operation_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
	{{ include('operation/_delete_form.html.twig') }}
  </div>
  <div class="form-container">
    <h1>Edit Operation</h1>

    {{ include('operation/_form.html.twig', { button_label: 'Update' }) }}
  </div>
{% endblock %}
