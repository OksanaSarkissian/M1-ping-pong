{% extends 'base.html.twig' %}

{% block title %}
  Operation
{% endblock %}

{% block body %}
  <div class="d-flex column-gap-2">
    <a href="{{ path('app_operation_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>

    <a href="{{ path('app_operation_edit', { id: operation.id }) }}"><button class="btn text-bg-accent-2 text-primary"><i class="fa-solid fa-pen"></i></button></a>

    {{ include('operation/_delete_form.html.twig') }}
  </div>

  <table class="table">
    <tbody>
      <tr>
        <th>Libelle</th>
        <td>{{ operation.libelle }}</td>
      </tr>
      <tr>
        <th>Description</th>
        <td>{{ operation.description }}</td>
      </tr>
      <tr>
        <th>Temps</th>
        <td>{{ operation.temps ? operation.temps|date('H:i:s') : '' }}</td>
      </tr>
      <tr>
        <th>Poste</th>
        <td>{{ operation.Poste.libelle }}</td>
      </tr>
      <tr>
        <th>Machine</th>
        <td>{{ operation.Machine.libelle }}</td>
      </tr>
    </tbody>
  </table>
  {% include './realisation/_form.html.twig' %}
{% endblock %}
