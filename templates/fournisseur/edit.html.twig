{% extends 'base.html.twig' %}

{% block title %}
  Modification Fournisseur
{% endblock %}

{% block body %}
  <div class="d-flex align-items-center">
    <a href="{{ path('app_fournisseur_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
    <h1>Modification Fournisseur</h1>
    {{ include('fournisseur/_delete_form.html.twig') }}
  </div>

  <div class="form-container">{{ include('fournisseur/_form.html.twig', { button_label: 'Enregistrer' }) }}</div>

{% endblock %}
