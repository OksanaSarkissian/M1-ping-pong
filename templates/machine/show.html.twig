{% extends 'base.html.twig' %}

{% block title %}Machine
{% endblock %}

{% block body %}
	<h1>Machine</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ machine.id }}</td>
			</tr>
			<tr>
				<th>Libelle</th>
				<td>{{ machine.libelle }}</td>
			</tr>
			<tr>
				<th>Postes</th>
				<td>
					{% for poste in machine.Poste %}
						{{ poste.libelle }},
					{% endfor %}
				</td>
			</tr>
		</tbody>
	</th>
</tr></tbody></table><a href="{{ path('app_machine_index') }}">back to list</a><a href="{{ path('app_machine_edit', {'id': machine.id}) }}"><button class="btn text-bg-accent-2 text-primary mx-3">
<i class="fa-solid fa-pen"></i></button></a>{{ include('machine/_delete_form.html.twig') }}{% endblock %}
