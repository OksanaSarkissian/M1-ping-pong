{% extends 'base.html.twig' %}

{% block title %}
  Machine
{% endblock %}

{% block body %}
  <div class="d-flex column-gap-2">
    <a href="{{ path('app_machine_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
    <a href="{{ path('app_machine_edit', { id: machine.id }) }}" class="btn text-bg-accent-2 text-primary"><i class="fa-solid fa-pen"></i></a>
    {{ include('machine/_delete_form.html.twig') }}
  </div>

  <table class="table w-50 h-50 m-auto my-auto">
    <tbody>
      <tr>
        <th>Id</th>
        <td>{{ machine.id }}</td>
      </tr>
      <tr>
        <th>Libelle</th>
        <td>{{ machine.libelle }}</td>
      </tr>
      <tr>
        <th>Postes</th>
        <td>
          {% for poste in machine.Poste %}
            {{ poste.libelle }},
          {% endfor %}
        </td>
      </tr>
    </tbody>
  </table>
{% endblock %}
