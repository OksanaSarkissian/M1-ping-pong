<form method="post" action="{{ path('app_machine_delete', {'id': machine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ machine.id) }}">
    <button class="btn text-bg-danger"><i class="fa-regular fa-trash-can"></i></button>
</form>
