{% extends 'base.html.twig' %}

{% block title %}Modifier Machine
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between align-items-center">
    <a href="{{ path('app_machine_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
		<h1>Modifier Machine</h1>
	{{ include('machine/_delete_form.html.twig') }}
  </div>
	<div class="form-container">

		{{ include('machine/_form.html.twig', {'button_label': 'Enregistrer'}) }}

	</div>
{% endblock %}
