{% extends 'base.html.twig' %}

{% block title %}Edit Machine
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between">
    <a href="{{ path('app_machine_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
	{{ include('machine/_delete_form.html.twig') }}
  </div>
	<div class="form-container">
		<h1>Edit Machine</h1>

		{{ include('machine/_form.html.twig', {'button_label': 'Update'}) }}

	</div>
{% endblock %}
