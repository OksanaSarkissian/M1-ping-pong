<form method="post" action="{{ path('app_gamme_delete', {'id': gamme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gamme.id) }}">
    <button class="btn text-bg-danger"><i class="fa-regular fa-trash-can"></i></button>
</form>
