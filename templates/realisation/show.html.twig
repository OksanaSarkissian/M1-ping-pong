{% extends 'base.html.twig' %}

{% block title %}Realisation{% endblock %}

{% block body %}
    <h1>Realisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ realisation.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ realisation.date ? realisation.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Temps_reel</th>
                <td>{{ realisation.tempsReel ? realisation.tempsReel|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_realisation_index') }}">back to list</a>

    <a href="{{ path('app_realisation_edit', {'id': realisation.id}) }}">edit</a>

    {{ include('realisation/_delete_form.html.twig') }}
{% endblock %}
