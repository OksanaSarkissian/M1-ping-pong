{% extends 'base.html.twig' %}

{% block title %}Edit Piece
{% endblock %}

{% block body %}
<div class="d-flex justify-content-between">
<a href="{{ path('app_piece_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a> {{ include('piece/_delete_form.html.twig') }}
</div>
	
	<div class="form-container">
		<h1>Edit Piece</h1>

		{{ include('piece/_form.html.twig', {'button_label': 'Update'}) }}
	</div>

	
{% endblock %}
