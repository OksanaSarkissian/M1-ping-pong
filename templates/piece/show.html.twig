{% extends 'base.html.twig' %}

{% block title %}Piece{% endblock %}

{% block body %}
    <h1>Piece</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ piece.id }}</td>
            </tr>
            <tr>
                <th>Reference_piece</th>
                <td>{{ piece.referencePiece }}</td>
            </tr>
            <tr>
                <th>Libelle_piece</th>
                <td>{{ piece.libellePiece }}</td>
            </tr>
            <tr>
                <th>Prix_u</th>
                <td>{{ piece.prixU }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ piece.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_piece_index') }}">back to list</a>

    <a href="{{ path('app_piece_edit', {'id': piece.id}) }}">edit</a>

    {{ include('piece/_delete_form.html.twig') }}
{% endblock %}
