{% extends 'base.html.twig' %}

{% block title %}
	Modification Client
{% endblock %}

{% block body %}
	<div class="d-flex align-items-center">
		<a href="{{ path('app_client_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
		<h1>Modification Client</h1>
		{{ include('client/_delete_form.html.twig') }}
	</div>
	<div class="form-container">{{ include('client/_form.html.twig', { button_label: 'Enregistrer' }) }}</div>
{% endblock %}
