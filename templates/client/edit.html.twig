{% extends 'base.html.twig' %}

{% block title %}
  Edit Client
{% endblock %}

{% block body %}
  <a href="{{ path('app_client_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
  <h1>Edit Client</h1>
  {{ include('client/_delete_form.html.twig') }}
  <div class="form-container">{{ include('client/_form.html.twig', { button_label: 'Update' }) }}</div>
{% endblock %}
