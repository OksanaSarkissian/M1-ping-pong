{% extends 'base.html.twig' %}

{% block title %}Edit Poste
{% endblock %}

{% block body %}
	<a href="{{ path('app_poste_index') }}">back to list</a>
	<div class="form-container">
		<h1>Edit Poste</h1>

		{{ include('poste/_form.html.twig', {'button_label': 'Update'}) }}
	</div>

	{{ include('poste/_delete_form.html.twig') }}
{% endblock %}
