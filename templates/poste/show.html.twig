{% extends 'base.html.twig' %}

{% block title %}Poste{% endblock %}

{% block body %}
    <div class="d-flex column-gap-2">
<a href="{{ path('app_poste_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>

    <a href="{{ path('app_poste_edit', {'id': poste.id}) }}"><button class="btn text-bg-accent-2 text-primary">
							<i class="fa-solid fa-pen"></i>
						</button></a>

    {{ include('poste/_delete_form.html.twig') }}
</div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poste.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ poste.libelle }}</td>
            </tr>
        </tbody>
    </table>

    
{% endblock %}
