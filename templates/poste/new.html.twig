{% extends 'base.html.twig' %}

{% block title %}
  New Poste
{% endblock %}

{% block body %}
  <div class="form-container">
    <h1>Create new Poste</h1>

    {{ include('poste/_form.html.twig') }}
  </div>
  <a href="{{ path('app_poste_index') }}">Retour à la liste</a>
{% endblock %}
