{% extends 'base.html.twig' %}

{% block title %}
  Edit Document
{% endblock %}

{% block body %}
  <h1>Modification Document</h1>
  <div class="form-container">{{ include('document/_form.html.twig', { button_label: 'Update' }) }}</div>

  <a href="{{ path('app_devis_index') }}">back to list</a>

  {{ include('document/_delete_form.html.twig') }}
{% endblock %}
