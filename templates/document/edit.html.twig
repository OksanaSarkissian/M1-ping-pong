{% extends 'base.html.twig' %}

{% block title %}
	Modification Document
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between align-items-center w-50">
		<div class="d-flex align-items-center justify-content-between w-100">
			<a class="btn text-bg-accent-2 text-primary" href="{{ path('app_devis_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
			<h1>Modification Document</h1>
		{{ include('document/_delete_form.html.twig') }}
		</div>
	</div>
	<div class="form-container">{{ include('document/_form.html.twig', { button_label: 'Enregistrer' }) }}</div>
{% endblock %}
