{% extends 'base.html.twig' %}

{% block title %}Droit{% endblock %}

{% block body %}
    <h1>Droit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ droit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ droit.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_droit_index') }}">back to list</a>

    <a href="{{ path('app_droit_edit', {'id': droit.id}) }}"><button class="btn text-bg-accent-2 text-primary mx-3">
							<i class="fa-solid fa-pen"></i>
						</button></a>

    {{ include('droit/_delete_form.html.twig') }}
{% endblock %}
