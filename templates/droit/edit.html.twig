{% extends 'base.html.twig' %}

{% block title %}Edit Droit
{% endblock %}

{% block body %}
	<a href="{{ path('app_droit_index') }}">back to list</a>
	<div class="form-container">
		<h1>Edit Droit</h1>

		{{ include('droit/_form.html.twig', {'button_label': 'Update'}) }}

	</div>
	{{ include('droit/_delete_form.html.twig') }}
{% endblock %}
