{% extends 'base.html.twig' %}

{% block title %}
  Modification Achat
{% endblock %}

{% block body %}
	<div class="d-flex align-items-center justify-content-between w-75">
  <a href="{{ path('app_achat_index') }}" class="btn bg-accent-2 text-primary">Retour à la liste</a>
  <h1>Modification Achat</h1>
  {{ include('achat/_delete_form.html.twig') }}
  </div>
  <div class="form-container">{{ include('achat/_form.html.twig', { button_label: 'Enregistrer' }) }}</div>


{% endblock %}
